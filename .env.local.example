# MongoDB Connection URI
# For local testing, you can use a local connection URI or a MongoDB Atlas connection string.
# Example Atlas connection string:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/redcheeks?retryWrites=true&w=majority

MONGODB_URI=mongodb://localhost:27017/redcheeks
